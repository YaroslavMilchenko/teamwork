import React from "react";

const Header = () => {
  return (
    <header style={styles.header}>
      <h1>Team 3</h1>
    </header>
  );
};

const styles = {
  header: {
    padding: "1em",
    fontSize: "2em",
    color: "#b156f2",
    textAlign: "center",
    fontWeight: "bold",
  },
};

export default Header;
